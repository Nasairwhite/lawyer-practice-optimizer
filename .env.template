# Lawyer Practice Optimization Diagnostic - Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# MOONSHOT AI API CONFIGURATION
# =============================================================================
# Get your API key from: https://platform.moonshot.cn/
MOONSHOT_API_KEY=your_moonshot_api_key_here

# =============================================================================
# FLASK APPLICATION CONFIGURATION
# =============================================================================
# Secret key for session management (generate a random string)
# Use: openssl rand -hex 32
FLASK_SECRET_KEY=your_secret_key_here_change_in_production

# Environment: development or production
FLASK_ENV=development

# Server port (default: 5000)
PORT=5000

# Base URL for generating diagnostic links
# For development: http://localhost:5000
# For production: https://yourdomain.com
BASE_URL=http://localhost:5000

# =============================================================================
# EMAIL CONFIGURATION (SMTP)
# =============================================================================
# Use Gmail or your preferred SMTP provider
# For Gmail: You may need to enable "Less secure app access" or use an App Password

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Email credentials
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_email_password_or_app_password

# From email and display name
FROM_EMAIL=your_email@gmail.com
FROM_NAME=Practice Optimization Diagnostic

# =============================================================================
# DIAGNOSTIC LINK SETTINGS
# =============================================================================
# Number of days before diagnostic links expire
LINK_EXPIRATION_DAYS=7

# =============================================================================
# OPTIONAL: REMINDER SETTINGS
# =============================================================================
# Send reminder if diagnostic not completed after X days
REMINDER_AFTER_DAYS=3

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================
# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# =============================================================================
# AI MODEL CONFIGURATION
# =============================================================================
# Moonshot model to use (default: moonshot-v1-8k)
# Options: moonshot-v1-8k, moonshot-v1-32k, moonshot-v1-128k
AI_MODEL=moonshot-v1-8k

# =============================================================================
# EXAMPLE SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file: cp .env.template .env
# 2. Edit .env and fill in your actual values
# 3. For Gmail SMTP, use an App Password if you have 2FA enabled
#    - Go to Google Account > Security > App passwords
#    - Generate a new app password for Mail
#    - Use that as your SMTP_PASSWORD
# 4. For FLASK_SECRET_KEY, generate a random string:
#    python -c "import secrets; print(secrets.token_hex(32))"
# 5. Set BASE_URL to your actual domain if deploying to production

# =============================================================================
# SECURITY NOTES
# =============================================================================
# - Never commit .env to version control (it should be in .gitignore)
# - Use strong, unique passwords for all services
# - In production, use environment variables set by your hosting provider
# - Consider using a dedicated email service like SendGrid, Mailgun, or AWS SES
# - Regularly rotate API keys and passwords
