{
  "name": "twilio-proxy",
  "version": "0.1.0",
  "description": "Twilio webhook relay, SMS proxy, and number-masking service with Claude AI handling.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/**/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@fastify/formbody": "^8.0.1",
    "@fastify/sensible": "^6.0.1",
    "dotenv": "^16.4.5",
    "fastify": "^5.0.0",
    "twilio": "^5.3.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "pino-pretty": "^11.2.2"
  }
}
